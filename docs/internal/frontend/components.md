# Frontend Components (Skeleton)

## Scope
Current frontend is debug-oriented (`public/debug.html`, `debug.css`, `debug.js`).

## Component inventory (debug UI)
- Map viewport and tile rendering
- Zoom controls
- Scroll/navigation controls
- Seed/map regeneration panel
- Console/log output panel

## TODO
- Document state model and fetch/prefetch flow.
- Document tile metadata rendering rules.
- Document UX constraints for map boundaries.
